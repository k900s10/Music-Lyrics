<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thelyrics">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/the_lyrics_ver1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/the_lyrics_ver1_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheLyrics">
        <activity
            android:name=".activity_lyric"
            android:exported="false" />
        <activity
            android:name=".search_title"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>